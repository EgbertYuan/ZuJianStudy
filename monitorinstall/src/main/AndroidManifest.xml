<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="cn.egbert.monitorinstall">

    <application
            android:label="@string/app_name"
            android:name=".InstallApplication"
            tools:replace="android:label">
        <activity android:name=".ui.MonitorActivity">

        </activity>
        <activity
                android:name=".ui.MonitorMainActivity"
                android:label="@string/mmodel_title_activity_monitor_main"

                android:theme="@style/mmodel_AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>